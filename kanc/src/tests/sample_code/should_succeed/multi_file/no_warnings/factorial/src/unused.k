This file isn't even syntactically valid.
However, if the build logic is implemented correctly,
it shouldn't even be included in the build,
so it should have absolutely no influence on whether
the build succeeds.
